import Flutter
import UIKit

// SceneDelegate is no longer used — UIApplicationSceneManifest was removed from
// Info.plist so the app uses the traditional AppDelegate lifecycle.  This file
// is kept to avoid stale Xcode references.
class SceneDelegate: FlutterSceneDelegate {
}
